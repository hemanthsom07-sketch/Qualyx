import PlaceholderPage from "./PlaceholderPage";

// Future implementation will call listProjects() from ../api/client
// and render real Project rows here.
function ProjectsPage() {
  return (
    <PlaceholderPage
      title="Projects"
      description="Project list UI (GET /projects) is not implemented in this stage."
    />
  );
}

export default ProjectsPage;
