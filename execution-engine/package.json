{
  "name": "qualyx-execution-engine",
  "version": "0.1.0",
  "description": "Qualyx deterministic step-execution prototype (Claude 2 — Task 4 milestone).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test tests/*.test.ts",
    "run-test-file": "tsx src/index.ts",
    "run-from-stdin": "tsx src/stdin-runner.ts"
  },
  "dependencies": {
    "playwright": "^1.46.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.15.0",
    "@types/node": "^20.14.0"
  }
}
