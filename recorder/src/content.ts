// Qualyx Recorder — content script (foundation only).
//
// This is intentionally a stub. It does NOT yet capture clicks, inputs,
// navigation, element/ARIA metadata, or DOM context. Full semantic event
// recording will be implemented once the RecordedJourney contract is
// finalized with Claude 2 (Backend) and Claude 3 (Intelligence).

console.log("[Qualyx Recorder] content script loaded (foundation build).");
